services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - "3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
      - ./comprovantes:/app/comprovantes

